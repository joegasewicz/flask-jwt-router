language: python
sudo: false

install:
  - pip install -U tox
  - pip install -U codecov

matrix:
  include:
    - python: "3.6-dev"
      env:
        - TOX_ENV=3.6-dev
    - python: "3.7-dev"
      env:
        - TOX_ENV=3.7-dev

script:
  - tox -v -e $TOX_ENV
  - codecov
